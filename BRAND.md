> **2026-08-13 갱신**: N-3.24 브랜드 리뉴얼(CPO 지시)로 사용자 대면 브랜드는
> `CartPilot`에서 `따져(TTAEJYO)`로 교체되었다. 이 문서는 최초 네이밍 결정
> 히스토리를 보존하기 위해 그대로 둔다 — 아래 내용은 과거 기록이다.

# Brand Decision

## 제품의 핵심 가치

이 서비스가 실제로 하는 일: 해외 상품 URL 하나를 입력받아

1. 이미지를 수집·정리하고(배경제거/표준화)
2. 상품 정보(제목/브랜드/가격/설명 등)를 추출하고
3. 플랫폼별(스마트스토어/쿠팡/11번가) 카테고리를 추천하고
4. AI가 한국어 상품명/설명/키워드/SEO를 생성해서
5. 스토어별 등록 화면 미리보기까지 만들어준다.

즉 사용자가 직접 하면 몇 시간 걸릴 "해외 상품 소싱 → 국내 마켓 등록 준비"를
URL 하나로 압축해주는 게 핵심 가치다. 사용자는 개발자가 아니라 셀러이므로,
Pipeline/Adapter/Provider 같은 내부 구조는 이름에도, 화면에도 드러나면 안 된다.

## 후보 10개

| 이름 | 의미 |
|---|---|
| **CartPilot** | Cart(커머스) + Pilot(AI 코파일럿) — "AI가 조종석 옆에서 등록을 도와준다"는 의미를 이름 자체가 전달한다. |
| Sourcefy | Source(소싱) + SaaS 흔한 접미사 "-fy". 소싱에 방점이 있어 등록 준비 단계(카테고리/콘텐츠)는 이름에서 안 드러남. |
| Listly | 리스팅(상품 등록) 생성 도구라는 의미는 명확하지만, 투두리스트 앱과 헷갈릴 여지가 있음. |
| Importly | 해외 상품 "수입 준비" 자동화라는 의미는 명확하지만 AI/콘텐츠 생성 뉘앙스가 약함. |
| StoreForge | "스토어 등록 화면을 벼려낸다"는 은유는 좋지만 Forge는 개발자 도구 이름에 이미 흔히 쓰여 SaaS보다 툴 느낌이 남. |
| MerchPilot | Merchandising + Pilot. CartPilot과 결이 비슷하지만 Merch가 "머천다이징"이라는 다소 전문 용어라 셀러에게 덜 직관적. |
| LaunchCart | 기존 프로젝트명 LaunchLens와 어휘를 공유해서 브랜드 연속성은 있지만, "Launch"가 이미 크라우드펀딩/제품출시 서비스들에 많이 쓰여 변별력이 약함. |
| SellReady | "판매 준비 완료"라는 상태를 그대로 이름으로 씀 — 직관적이지만 너무 서술적이라 SaaS 제품명보다는 상태 라벨처럼 들림(실제로 이번 Mission에서 상태 라벨로 쓰기도 함). |
| CatalogAI | 이해하기 쉽지만 "무엇+AI" 조합형 이름이 이미 시장에 매우 흔해서 변별력이 낮음. |
| ShelfReady | "매대에 올릴 준비"라는 은유는 좋으나 국내 사용자에게 "Shelf"의 이미지가 약하고 발음도 다소 김. |

## 최종 선정: **CartPilot**

**이유**
- PM이 제시한 포지셔닝("Linear + Notion + Shopify Admin + AI Copilot")을 이름 구조 자체가 그대로 반영한다 — Cart(커머스 정체성)와 Pilot(AI가 옆에서 조종을 돕는다는 감각)이 합성어 안에 다 들어있다.
- 두 단어 다 글로벌하게 통용되는 쉬운 영단어라 발음/기억이 쉽고, 한국 사용자에게도 "카트파일럿"으로 자연스럽게 읽힌다.
- 실제 화면 카피("AI가 상품을 분석하고 있습니다", "다음 단계") 톤과 자연스럽게 붙는다 — "카트파일럿이 상품을 분석했습니다" 같은 문장이 어색하지 않다.
- 기존 코드의 `@commerce/*` 워크스페이스 패키지명, GitHub 저장소명(`commerce-platform`)과 충돌하지 않는다 — 이번 Mission에서는 패키지/저장소명을 바꾸지 않고, 사용자 화면(제목, 헤더, 메타데이터, 카피)에만 브랜드를 적용한다. 패키지 리네임은 임포트 경로 수십 곳을 건드리는 별도 리스크가 큰 작업이라 이번 범위에서 제외한다.

**LaunchLens는 유지하는가, 교체하는가**
LaunchLens는 이번 Mission 이전까지 PM이 프로젝트를 지칭할 때 써온 이름이지만, 실제 코드베이스 어디에도 문자열로 박혀있지 않다(패키지명은 전부 `@commerce/*`). 그래서 "교체"라기보다 "처음으로 사용자 화면에 실제 브랜드를 붙이는 것"에 가깝다. 최종 결론: **CartPilot을 사용자 대면 브랜드명으로 확정**하고, LaunchLens는 더 이상 쓰지 않는다.

## 포지셔닝 한 줄

> **CartPilot — 해외 상품 URL 하나로, AI가 국내 마켓 등록 준비를 끝내는 커머스 코파일럿.**
